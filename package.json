{
  "name": "ubuntu-components",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build -w",
    "clean": "rm -rf packages/*/dist",
    "create": "node scripts/create-package.js"
  },
  "devDependencies": {
    "@types/react": "^19.2.14",
    "react": "^19.2.4",
    "tsup": "^8.x",
    "typescript": "^5.x"
  }
}
